# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

target 'Snapples' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Snapples
  pod 'SwiftyJSON'
  pod 'Alamofire', '~> 4.4'
  pod 'Kingfisher', '~> 4.0'

  target 'SnapplesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SnapplesUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
